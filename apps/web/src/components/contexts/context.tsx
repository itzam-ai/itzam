import { Contexts } from "@itzam/server/db/contexts/actions";

export const Context = ({ context }: { context: Contexts[number] }) => {
  return {
    /* <FileInput
          workflowId={workflowId}
          resources={contexts.flatMap((context) => context.resources)}
          contextId={contexts.id}
        />
        <LinkInput
          workflowId={workflowId}
          resources={contexts.flatMap((context) => context.resources)}
          contextId={contexts.id}
        /> */
  };
};
